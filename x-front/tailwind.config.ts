import type { Config } from "tailwindcss"

const config = {
  darkMode: ["class"],
  content: [
    './pages/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
    './app/**/*.{ts,tsx}',
    './src/**/*.{ts,tsx}',
	],
  prefix: "",
  theme: {
    container: {
      center: true,
      padding: "2rem",
      screens: {
        "2xl": "1400px",
      },
    },
    extend: {
      backgroundImage: theme => ({
        'dark-gradient': 'linear-gradient(to bottom, #0f0c29, #302b63, #24243e)',
      }),
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        // Primary Brand Color: Use for primary action items, links, and icons.
        'deep-sky-blue': '#007bff',

        // Secondary Brand Color: Use for secondary actions, informational callouts, and hover states.
        'cerulean-blue': '#2A9DF4',

        // Accent Color: Use sparingly to draw attention to key actions like call-to-action buttons or important icons.
        'sunset-orange': '#FF5E57',

        // Background Color: Ideal for light mode backgrounds, card backgrounds, and any area needing contrast against text.
        'cultured': '#F8F9FA',

        // Dark Background Color: Use for headers, footers, and dark mode themes. Provides a strong contrast against light text.
        'gunmetal': '#243B55',

        // Primary Text Color: Use for primary text content. Offers high readability on light backgrounds.
        'rich-black': '#011627',

        // Secondary Text Color: Use for less important text, placeholders, or to provide a softer contrast against light backgrounds.
        'sonic-silver': '#757575',
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
      keyframes: {
        "accordion-down": {
          from: { height: "0" },
          to: { height: "var(--radix-accordion-content-height)" },
        },
        "accordion-up": {
          from: { height: "var(--radix-accordion-content-height)" },
          to: { height: "0" },
        },
      },
      animation: {
        "accordion-down": "accordion-down 0.2s ease-out",
        "accordion-up": "accordion-up 0.2s ease-out",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
} satisfies Config

export default config