import { UserInfo } from "@/models/user"

interface MediaManagementProps {
    userInfo: UserInfo
}

function MediaManagement({ userInfo }: MediaManagementProps) {
    return <>
    </>
}

export default MediaManagement